# Benchmark protocol

No production benchmark is claimed without an executable runtime. Once implemented, measure p50/p95/p99 latency, throughput, error rate, concurrency, resource saturation and domain-specific correctness/regression metrics with a pinned workload and source revision.